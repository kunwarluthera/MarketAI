# Risk governance

Risk settings are persisted as versioned JSON with effective time, actor, and reason. Current
settings include capital/risk limits, loss limits, exposure, liquidity, spread, consecutive losses,
cooldown, expiry windows, averaging, EOD exits, kill switch, and reconciliation safety blocking.
`LIVE_TRADING_ENABLED` remains false.
